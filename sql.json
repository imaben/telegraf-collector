{
    "sql" : [
        {
            "table" : "visit",
            "sql" : "select count(1) as visit_count from visit where created_at >= '{START_TIME}' and created_at <= '{END_TIME}'",
            "frequency" : 5
        }
    ]
}
